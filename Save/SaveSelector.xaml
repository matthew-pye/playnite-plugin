<PluginUserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" x:Class="RomM.Save.RomMSaveSelector"
            xmlns:local="clr-namespace:RomM.Save"
            DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Background="Transparent">

        <DockPanel>
        <StackPanel x:Name="SelectorPanel">
            <Separator Margin="0,5,0,0" />
            <ScrollViewer DockPanel.Dock="Top" Width="auto" Height="120" Margin="5,5,5,5" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="tStack">
                    <ItemsControl ItemsSource="{Binding Saves}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton Margin="5,5,5,5" GroupName="Options" IsChecked="{Binding IsSelected, Mode=TwoWay}" Content="{Binding File.Name, Mode=OneWay}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
            <Grid Margin="5,5,5,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>
                <Button Name="Never_btn" Click="Click_Never" Grid.Row="0" Grid.Column="0" MaxWidth="75" HorizontalAlignment="Left" Margin="5,0,0,0" Background="#808080">
                    <Label Content="Never Save" Grid.Row="0" Grid.Column="0"/>
                </Button>
                <Button Name="Dont_btn" Click="Click_DontSave" Grid.Row="0" Grid.Column="1" Margin="5,0,0,0" Background="#bb0000">
                    <Label Content="Don't Save" Grid.Row="0" Grid.Column="0"/>
                </Button>
                <Button Name="Save" Click="Click_Save" Grid.Row="0" Grid.Column="2" Margin="5,0,0,0" Background="#45a240">
                    <Label Content="Save" Grid.Row="0" Grid.Column="0"/>
                </Button>

            </Grid>
        </StackPanel>
    </DockPanel>
</PluginUserControl>
