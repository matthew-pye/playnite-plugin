<UserControl x:Class="Graviton.Settings.SingleNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:s="clr-namespace:Graviton.Settings"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="750">

    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="LightGray" />
        </Style>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="LightGray" />
        </Style>
        
        <Storyboard x:Key="SlideInAnimation">
            <DoubleAnimation 
                Storyboard.TargetName="RootBorder"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                From="-80" To="0"
                Duration="0:0:0.35"
            >
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            
            <DoubleAnimation 
                Storyboard.TargetName="RootBorder"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1"
                Duration="0:0:0.25"
            />
        </Storyboard>

        <Storyboard x:Key="SlideOutAnimation" Completed="SlideOut_Completed">
            <DoubleAnimation Storyboard.TargetName="RootBorder"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="0" To="-80"
                             Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="RootBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="1" To="0"
                             Duration="0:0:0.25"/>
        </Storyboard>

        <Storyboard x:Key="ProgressBarAnimation" Completed="Progress_Completed">
            <DoubleAnimation
                Storyboard.TargetName="ProgressBar"
                Storyboard.TargetProperty="Width"
                From="336" To="0"
                Duration="0:0:4"
            />
        </Storyboard>

    </UserControl.Resources>

    
    <Border x:Name="RootBorder" CornerRadius="8" Margin="0,0,0,8" ClipToBounds="True" Opacity="0" SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="6" Opacity="0.45" Direction="270"/>
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform Y="-80"/>
        </Border.RenderTransform>

        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="{Binding AccentColour}"/>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="4"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding Icon}" Foreground="{Binding TextColour}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,2,0,2"/>
                <TextBlock Grid.Column="1" Text="{Binding Message}" Foreground="{Binding TextColour}" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Margin="10,0,0,0"/>

                <Button Grid.Column="2" x:Name="CloseButton" Click="CloseButton_Click" Width="16" Height="16" VerticalAlignment="Center" Margin="8,0,0,0" Cursor="Hand" >
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                            CornerRadius="12">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#33FFFFFF"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="✕" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                </Button>
            </Grid>

            <Border Grid.Row="1" Background="#22FFFFFF" Height="4">
                <Rectangle x:Name="ProgressBar" Height="4" HorizontalAlignment="Left" Fill="{Binding ProgressBarColour}"/>
            </Border>

        </Grid>
    </Border>

</UserControl>
