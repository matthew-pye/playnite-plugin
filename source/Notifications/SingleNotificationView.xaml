<UserControl x:Class="Graviton.Settings.SingleNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:s="clr-namespace:Graviton.Settings"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="750">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Graviton;component/ThemingDefaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Storyboard x:Key="SlideInAnimation">
                <DoubleAnimation 
        Storyboard.TargetName="RootBorder"
        Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
        From="-80" To="0"
        Duration="0:0:0.35">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>

                <DoubleAnimation 
        Storyboard.TargetName="RootBorder"
        Storyboard.TargetProperty="Opacity"
        From="0" To="1"
        Duration="0:0:0.25"/>
            </Storyboard>

            <Storyboard x:Key="SlideOutAnimation" Completed="SlideOut_Completed">
                <DoubleAnimation Storyboard.TargetName="RootBorder"
                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                     From="0" To="-80"
                     Duration="0:0:0.3">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseIn"/>
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="RootBorder"
                     Storyboard.TargetProperty="Opacity"
                     From="1" To="0"
                     Duration="0:0:0.25"/>
            </Storyboard>

            <Storyboard x:Key="ProgressBarAnimation" Completed="Progress_Completed">
                <DoubleAnimation
        Storyboard.TargetName="ProgressBar"
        Storyboard.TargetProperty="Width"
        From="336" To="0"
        Duration="0:0:4"/>
            </Storyboard>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border x:Name="RootBorder" CornerRadius="8" Margin="0,0,0,8" ClipToBounds="True" Opacity="0" SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="6" Opacity="0.45" Direction="270"/>
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform Y="-80"/>
        </Border.RenderTransform>

        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding WPFSeverity}" Value="Success">
                        <Setter Property="Background" Value="{DynamicResource Graviton.SuccessBackground.Colour}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding WPFSeverity}" Value="Warn">
                        <Setter Property="Background" Value="{DynamicResource Graviton.WarnBackground.Colour}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding WPFSeverity}" Value="Error">
                        <Setter Property="Background" Value="{DynamicResource Graviton.ErrorBackground.Colour}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding WPFSeverity}" Value="Info">
                        <Setter Property="Background" Value="{DynamicResource Graviton.InfoBackground.Colour}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="4"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" FontFamily="{StaticResource NerdFont}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,2,0,2">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Success">
                                    <Setter Property="Text" Value="&#xf00c;" />
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Success.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Warn">
                                    <Setter Property="Text" Value="&#xea6c;" />
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Warn.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Error">
                                    <Setter Property="Text" Value="&#xf015d;" />
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Error.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Info">
                                    <Setter Property="Text" Value="&#xf449;" />
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Info.Colour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Grid.Column="1" Text="{Binding WPFMessage}" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Margin="10,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Success">
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Success.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Warn">
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Warn.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Error">
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Error.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Info">
                                    <Setter Property="Foreground" Value="{DynamicResource Graviton.Info.Colour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Grid.Column="2" x:Name="CloseButton" Click="CloseButton_Click" Width="16" Height="16" VerticalAlignment="Center" Margin="8,0,0,0" Cursor="Hand" >
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding WPFSeverity}" Value="Success">
                                                            <Setter Property="Background" Value="{DynamicResource Graviton.SuccessBackground.Colour}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding WPFSeverity}" Value="Warn">
                                                            <Setter Property="Background" Value="{DynamicResource Graviton.WarnBackground.Colour}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding WPFSeverity}" Value="Error">
                                                            <Setter Property="Background" Value="{DynamicResource Graviton.ErrorBackground.Colour}" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding WPFSeverity}" Value="Info">
                                                            <Setter Property="Background" Value="{DynamicResource Graviton.InfoBackground.Colour}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#33FFFFFF"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="✕" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                </Button>
            </Grid>

            <Border Grid.Row="1" Background="#22FFFFFF" Height="4">
                <Rectangle x:Name="ProgressBar" Height="4" HorizontalAlignment="Left">
                    <Rectangle.Style>
                        <Style TargetType="Rectangle">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Success">
                                    <Setter Property="Fill" Value="{DynamicResource Graviton.SuccessAccent.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Warn">
                                    <Setter Property="Fill" Value="{DynamicResource Graviton.WarnAccent.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Error">
                                    <Setter Property="Fill" Value="{DynamicResource Graviton.ErrorAccent.Colour}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding WPFSeverity}" Value="Info">
                                    <Setter Property="Fill" Value="{DynamicResource Graviton.InfoAccent.Colour}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Rectangle.Style>
                </Rectangle>
            </Border>

        </Grid>
    </Border>

</UserControl>
