<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:local="clr-namespace:Graviton"
                    xmlns:playnite="clr-namespace:Playnite;assembly=Playnite.SDK">

    <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    <local:ImageCacheConverter x:Key="imageCacheConverter" />
    <local:EnumDescriptionConverter x:Key="EnumDescConverter"/>

    <SolidColorBrush x:Key="Graviton.RomM.Colour" Color="#372968"/>
    <SolidColorBrush x:Key="Graviton.RomMAccent.Colour" Color="#b9a6ce"/>

    <!-- Was hardcoded inline in SettingsTabControl.xaml for the active/inactive tab indicator.
         Named here so it's a single source of truth, but note this is a different, brighter purple
         than Graviton.RomM.Colour above - see chat for whether these should be consolidated. -->
    <SolidColorBrush x:Key="Graviton.Accent.Colour" Color="#8b74e8"/>
    <SolidColorBrush x:Key="Graviton.AccentMuted.Colour" Color="#524489"/>
    
    <SolidColorBrush x:Key="Graviton.Success.Colour" Color="#4ade80"/>
    <SolidColorBrush x:Key="Graviton.SuccessAccent.Colour" Color="#4ade80"/>
    <SolidColorBrush x:Key="Graviton.SuccessBackground.Colour" Color="#1a3e2e"/>

    <SolidColorBrush x:Key="Graviton.Warn.Colour" Color="#facc15"/>
    <SolidColorBrush x:Key="Graviton.WarnAccent.Colour" Color="#facc15"/>
    <SolidColorBrush x:Key="Graviton.WarnBackground.Colour" Color="#413a17"/>

    <SolidColorBrush x:Key="Graviton.Error.Colour" Color="#ff5050"/>
    <SolidColorBrush x:Key="Graviton.ErrorAccent.Colour" Color="#ff5050"/>
    <SolidColorBrush x:Key="Graviton.ErrorBackground.Colour" Color="#421f24"/>

    <SolidColorBrush x:Key="Graviton.Info.Colour" Color="#93c5fd"/>
    <SolidColorBrush x:Key="Graviton.InfoAccent.Colour" Color="#93c5fd"/>
    <SolidColorBrush x:Key="Graviton.InfoBackground.Colour" Color="#2a394a"/>

    <SolidColorBrush x:Key="Graviton.Special.Colour" Color="#c084fc"/>
    <SolidColorBrush x:Key="Graviton.SpecialAccent.Colour" Color="#c084fc"/>
    <SolidColorBrush x:Key="Graviton.SpecialBackground.Colour" Color="#332347"/>

    <SolidColorBrush x:Key="Graviton.Text.Colour" Color="#ffffff"/>
    <SolidColorBrush x:Key="Graviton.SubText.Colour" Color="#bfffffff"/>
    <SolidColorBrush x:Key="Graviton.SubSubText.Colour" Color="#73ffffff"/>
    <SolidColorBrush x:Key="Graviton.Trace.Colour" Color="#40ffffff"/>

    <SolidColorBrush x:Key="Graviton.Background.Colour" Color="#ffffff"/>
    <SolidColorBrush x:Key="Graviton.DarkBackground.Colour" Color="#1f1f1f"/>
    <SolidColorBrush x:Key="Graviton.TransparentBackground.Colour" Color="#12ffffff"/>
    <SolidColorBrush x:Key="Graviton.TransparentMouseOverBackground.Colour" Color="#26ffffff"/>
    <SolidColorBrush x:Key="Graviton.DarkTransparentBackground.Colour" Color="#121f1f1f"/>
    <SolidColorBrush x:Key="Graviton.DarkTransparentMouseOverBackground.Colour" Color="#361f1f1f"/>

    <x:Static x:Key="NerdFont" Member="playnite:Fonts.NerdFont"/>
    <x:Static x:Key="IcoFont" Member="playnite:Fonts.IcoFont"/>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="FontSize" Value="14" />
    </Style>
    <Style TargetType="{x:Type Expander}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
    </Style>
    <Style TargetType="{x:Type Separator}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
    </Style>

    <!-- #region Button-->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Padding" Value="10, 5" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                        <ContentPresenter Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Graviton.TransparentMouseOverBackground.Colour}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region Textbox -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Graviton.SubText.Colour}" />
        <Setter Property="Padding" Value="5,5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                        <Grid>
                            <TextBlock x:Name="PlaceholderText" Text="{Binding Path=(local:Placeholder.Text), RelativeSource={RelativeSource TemplatedParent}}" Foreground="{DynamicResource Graviton.SubSubText.Colour}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region Checkbox -->
    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Border" Width="18" Height="18" Background="Transparent" BorderBrush="{DynamicResource Graviton.TransparentMouseOverBackground.Colour}" BorderThickness="1" CornerRadius="4" SnapsToDevicePixels="True">
                            <Grid>
                                <Path x:Name="Check" Data="M 2,7 L 6,11 L 13,3" Stroke="{DynamicResource Graviton.Text.Colour}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"  StrokeLineJoin="Round" Visibility="Collapsed"/>
                                <Rectangle x:Name="Indeterminate" Width="8" Height="8" RadiusX="1" RadiusY="1" Fill="{DynamicResource Graviton.Text.Colour}" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="Indeterminate" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region ToggleButton -->
    <Style TargetType="{x:Type ToggleButton}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="Root" Width="44" Height="24">
                        <Border x:Name="TrackOff" CornerRadius="12" Background="{DynamicResource Graviton.TransparentMouseOverBackground.Colour}" BorderBrush="Transparent" BorderThickness="1" SnapsToDevicePixels="True" />
                        <Border x:Name="TrackOn" CornerRadius="12" Background="{DynamicResource Graviton.RomM.Colour}" Opacity="0" SnapsToDevicePixels="True" />
                        <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0,0,0" SnapsToDevicePixels="True">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.35" Direction="270" Color="Black" />
                            </Ellipse.Effect>
                            <Ellipse.RenderTransform>
                                <TranslateTransform x:Name="ThumbTransform" X="0" />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="TrackOn" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="TrackOn" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TrackOff" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region ComboBox -->
    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="Padding" Value="2,5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border" Background="Transparent" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5, 5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="Toggle" Background="Transparent" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" ClickMode="Press" Focusable="False">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="24"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" Data="M 0,0 L 4,4 L 8,0" Stroke="{DynamicResource Graviton.Text.Colour}" StrokeThickness="1.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                        <TextBlock x:Name="Placeholder" Text="{TemplateBinding Tag}" IsHitTestVisible="False" Foreground="{DynamicResource Graviton.SubSubText.Colour}"  Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed"/>

                        <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                            <Border Background="{DynamicResource Graviton.DarkBackground.Colour}" BorderBrush="{DynamicResource Graviton.DarkBackground.Colour}" BorderThickness="1" CornerRadius="6" Margin="0,4,0,0" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="300" SnapsToDevicePixels="True">
                                <ScrollViewer SnapsToDevicePixels="True">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Toggle" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="SelectedIndex" Value="-1">
                            <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region ListBox -->
    <Style TargetType="{x:Type ListBoxItem}">
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Graviton.TransparentMouseOverBackground.Colour}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Graviton.TransparentMouseOverBackground.Colour}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                        <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter SnapsToDevicePixels="True" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region Passwordbox -->
    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Graviton.SubText.Colour}" />
        <Setter Property="Padding" Value="2,5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" SnapsToDevicePixels="True">
                        <Grid>
                            <TextBlock x:Name="PlaceholderText" Text="{Binding Path=(local:Placeholder.Text), RelativeSource={RelativeSource TemplatedParent}}" Foreground="{DynamicResource Graviton.SubSubText.Colour}" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=(local:Placeholder.HasText), RelativeSource={RelativeSource TemplatedParent}}" Value="False">
                            <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource Graviton.Text.Colour}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region Slider -->
    <Style x:Key="Graviton.SliderRepeatButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" Height="6" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Graviton.SliderFillRepeatButton" TargetType="{x:Type RepeatButton}" BasedOn="{StaticResource Graviton.SliderRepeatButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{DynamicResource Graviton.Text.Colour}" Height="6" CornerRadius="3" VerticalAlignment="Center" SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Graviton.SliderThumb" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Width" Value="16" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Grid>
                        <!-- Value bubble: floats above the thumb, hidden until hover/drag/click -->
                        <Border x:Name="ValueBubble" Background="{DynamicResource Graviton.DarkBackground.Colour}" BorderBrush="{DynamicResource Graviton.TransparentBackground.Colour}" BorderThickness="1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-28,0,0" Visibility="Collapsed" SnapsToDevicePixels="True" IsHitTestVisible="False">
                            <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType={x:Type Slider}}, StringFormat={}{0:0.##}}" Foreground="{DynamicResource Graviton.Text.Colour}" FontSize="11" FontWeight="SemiBold" />
                        </Border>
                        <Ellipse x:Name="Ellipse" Fill="{DynamicResource Graviton.Text.Colour}" Stroke="{DynamicResource Graviton.DarkBackground.Colour}" StrokeThickness="1" SnapsToDevicePixels="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Ellipse" Property="StrokeThickness" Value="2" />
                            <Setter TargetName="ValueBubble" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="Ellipse" Property="Opacity" Value="0.8" />
                            <Setter TargetName="ValueBubble" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}" Value="True">
                            <Setter TargetName="ValueBubble" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Slider}">
        <Setter Property="Background" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Slider}">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TickBar x:Name="TopTick" Grid.Row="0" Grid.Column="1" Placement="Top" Height="8" Margin="8,0" Opacity="0.5"
                             Fill="{TemplateBinding Foreground}" Visibility="Collapsed" SnapsToDevicePixels="True"
                             Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" TickFrequency="{TemplateBinding TickFrequency}"
                             IsDirectionReversed="{TemplateBinding IsDirectionReversed}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0"
                             Foreground="{DynamicResource Graviton.SubText.Colour}"
                             Text="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0:0.##}}" />

                        <Grid Grid.Row="1" Grid.Column="1" Height="24" VerticalAlignment="Center">
                            <Border Height="6" Background="{TemplateBinding Background}" CornerRadius="3" VerticalAlignment="Center" SnapsToDevicePixels="True" />
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource Graviton.SliderFillRepeatButton}" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource Graviton.SliderRepeatButton}" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource Graviton.SliderThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>

                        <TextBlock Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" FontSize="12" Margin="8,0,0,0"
                             Foreground="{DynamicResource Graviton.SubText.Colour}"
                             Text="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}, StringFormat={}{0:0.##}}" />

                        <TickBar x:Name="BottomTick" Grid.Row="2" Grid.Column="1" Placement="Bottom" Height="8" Margin="8,0" Opacity="0.5"
                             Fill="{TemplateBinding Foreground}" Visibility="Collapsed" SnapsToDevicePixels="True"
                             Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" TickFrequency="{TemplateBinding TickFrequency}"
                             IsDirectionReversed="{TemplateBinding IsDirectionReversed}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TickPlacement" Value="TopLeft">
                            <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="TickPlacement" Value="BottomRight">
                            <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="TickPlacement" Value="Both">
                            <Setter TargetName="TopTick" Property="Visibility" Value="Visible" />
                            <Setter TargetName="BottomTick" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- #endregion -->

    <!-- #region Settings layout helpers -->
    <Style x:Key="Graviton.SettingsCard" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{DynamicResource Graviton.DarkTransparentBackground.Colour}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Graviton.TransparentBackground.Colour}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <Style x:Key="Graviton.SettingsSectionHeader" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.Text.Colour}" />
        <Setter Property="Margin" Value="0,0,0,14" />
    </Style>

    <Style x:Key="Graviton.SettingsSubHeader" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.SubText.Colour}" />
        <Setter Property="Margin" Value="0,10,0,10" />
    </Style>

    <Style x:Key="Graviton.SettingsHelperText" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.SubSubText.Colour}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,2,0,8" />
    </Style>

    <Style x:Key="Graviton.SettingsToggleRow" TargetType="{x:Type Grid}">
        <Setter Property="Margin" Value="0,7" />
    </Style>
    <!-- #endregion -->

    <!-- #region Icon Button -->
    <Style x:Key="Graviton.IconButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Graviton.SubText.Colour}" />
        <Setter Property="Opacity" Value="0.6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- #endregion -->

</ResourceDictionary>