<UserControl ClipToBounds="True"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    x:Class="RomM.Settings.SettingsView"
    xmlns:local="clr-namespace:RomM.Settings"
    xmlns:enum="clr-namespace:RomM.Models.RomM.Rom"
    xmlns:draw="clr-namespace:System.Drawing;assembly=System.Drawing"
    xmlns:sys="clr-namespace:System;assembly=mscorlib" mc:Ignorable="d" d:DesignHeight="1000" d:DesignWidth="800" Padding="2,0,2,4">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <local:ImageCacheConverter x:Key="imageCacheConverter" />
    </UserControl.Resources>

    <DockPanel Margin="20">
        <Grid>
            <!-- Notification Bar -->
            <StackPanel VerticalAlignment="Top" Margin="2,2,2,2" MinHeight="22" MaxHeight="22" HorizontalAlignment="Stretch">
                <!-- Notification Bar Animation -->
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <TranslateTransform Y="-60" />
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Notify}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                From="-60" To="-22" 
                                                Duration="0:0:0.4"/>
                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                From="-22" To="-22" 
                                                Duration="0:0:10"
                                                BeginTime="0:0:10.4"/>

                                            <DoubleAnimation 
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                From="-22" To="-60" 
                                                Duration="0:0:0.1"
                                                BeginTime="0:0:10.5"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Border Grid.Row="0" Grid.Column="0" MaxHeight="22" MinHeight="22" Width="Auto" CornerRadius="3">
                    <Border.Background>
                        <SolidColorBrush Color="{Binding NotfiyColour}" Opacity="0.3" />
                    </Border.Background>
                    <Grid VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="{Binding NotifyIcon, Mode=OneWay}" Foreground="{Binding NotfiyTextColour, Mode=OneWay}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding NotifyText, Mode=OneWay}" Foreground="{Binding NotfiyTextColour, Mode=OneWay}" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>

            <TabControl>
                <!-- Settings Panel -->
                <TabItem Name="Settings" Header="Settings">
                    <ScrollViewer DockPanel.Dock="Top">
                        <StackPanel x:Name="EmulatorSettingsPanel">
                            <TextBlock Text="Authentication" FontWeight="Bold" Margin="0,5,0,0"/>
                            <Separator Margin="0,5,0,10" />
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="255"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="60"/>
                                    <RowDefinition Height="60"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                </Grid.RowDefinitions>

                                <!-- RomM User data -->
                                <Image Name="RomMProfileIcon" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Source="{Binding ProfilePath, Mode=OneWay, Converter={StaticResource imageCacheConverter}}" Width="120" HorizontalAlignment="Left" Margin="5,5,0,5" />
                                <Label Name="RomMUser" Content="{Binding RomMUser, Mode=OneWay}" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" FontSize="22" VerticalAlignment="Bottom" Margin="-30,0,0,0"/>
                                <Label Name="RomMProfileType" Content="{Binding RomMProfileType, Mode=OneWay}" Grid.Row="1" Grid.Column="1" Margin="-30,0,0,0" Foreground="DimGray" FontSize="12" VerticalAlignment="Top"/>
                                <Button Name="TestServer" Click="Click_TestConnection" Grid.Row="1" Grid.Column="3" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top">
                                    <TextBlock Text="Authenticate" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>

                                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Top">
                                    <Label Name="RomMServerVersionText" Content="Server Ver." Foreground="DimGray" FontSize="12" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                    <Label Name="RomMServerVersion" Content="{Binding ServerVersion, Mode=OneWay}" Grid.Row="0" Grid.Column="3" Margin="0" Foreground="DimGray" FontSize="12" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                </StackPanel>

                                <!-- RomM Host -->
                                <TextBlock Text="RomM Host" Grid.Row="2" Grid.Column="0" Margin="0,0,5,5"/>
                                <TextBox x:Name="RomMHost" Grid.Row="2" Grid.Column="1" Margin="2" Width="250" MaxWidth="250" HorizontalAlignment="Left" Text="{Binding RomMHost, Mode=TwoWay}" />

                                <!-- RomM Client Token -->
                                <TextBlock Text="RomM Client Token (&gt;=4.8)" Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center"/>
                                <TextBox x:Name="RomMApiKey" Grid.Row="3" Grid.Column="1" Margin="2" Width="250" MaxWidth="250" HorizontalAlignment="Left" Text="{Binding RomMClientToken, Mode=TwoWay}" />
                                <TextBlock Grid.Row="3" Grid.Column="2" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Hyperlink NavigateUri="{Binding ClientTokenURL}" RequestNavigate="Hyperlink_RequestNavigate">
                                        Get Token
                                    </Hyperlink>
                                </TextBlock>

                                <CheckBox IsChecked="{Binding UseBasicAuth, Mode=TwoWay}" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="Use Basic Auth" TextWrapping="Wrap" FontSize="12"/>
                                </CheckBox>

                                <!-- Username -->
                                <TextBlock Text="Username" Grid.Row="5" Grid.Column="0" Margin="40,0,5,5" Visibility="{Binding UseBasicAuth, Converter={StaticResource BoolToVisConverter}}"/>
                                <TextBox x:Name="RomMUsername" Grid.Row="5" Grid.Column="1" Margin="2" Width="250" MaxWidth="250" HorizontalAlignment="Left" Text="{Binding RomMUsername, Mode=TwoWay}" Visibility="{Binding UseBasicAuth, Converter={StaticResource BoolToVisConverter}}"/>

                                <!-- Password -->
                                <TextBlock Text="Password" Grid.Row="6" Grid.Column="0" Margin="40,5,5,0"  Visibility="{Binding UseBasicAuth, Converter={StaticResource BoolToVisConverter}}"/>
                                <TextBox x:Name="RomMPassword" Grid.Row="6" Grid.Column="1" Margin="2" Width="250" MaxWidth="250" HorizontalAlignment="Left" Text="{Binding RomMPassword, Mode=TwoWay}" Visibility="{Binding UseBasicAuth, Converter={StaticResource BoolToVisConverter}}"/>
                            </Grid>
                            <TextBlock Text="Emulator path mappings" FontWeight="Bold" />
                            <TextBlock HorizontalAlignment="Right" Margin="0,-16,0,0">
                                <Hyperlink NavigateUri="https://github.com/rommapp/playnite-plugin/blob/main/README.md" RequestNavigate="Hyperlink_RequestNavigate">
                                    Help
                                </Hyperlink>
                            </TextBlock>
                            <Separator Margin="0,5,0,10" />

                            <!-- Emulator Mappings -->
                            <StackPanel x:Name="MappingsStack">
                                <ItemsControl ItemsSource="{Binding Mappings}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Expander Header="{Binding MappingName, Mode=OneWay}">
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="250"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="26"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="30"/>
                                                        <RowDefinition Height="30"/>
                                                        <RowDefinition Height="30"/>
                                                        <RowDefinition Height="30"/>
                                                        <RowDefinition Height="30"/>
                                                        <RowDefinition Height="10"/>
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="Emulator" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <ComboBox Name="ComboEmulators" Grid.Row="0" Grid.Column="1" Margin="3,2,0,2" DockPanel.Dock="Left" ItemsSource="{Binding AvailableEmulators}" DisplayMemberPath="Name" SelectedItem="{Binding Emulator, Mode=TwoWay}"/>

                                                    <TextBlock Text="Profile" Grid.Row="1" Grid.Column="0" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <ComboBox Name="ComboPlatforms" Grid.Row="1" Grid.Column="1" Margin="3,2,0,2" DockPanel.Dock="Left" ItemsSource="{Binding AvailableProfiles}" DisplayMemberPath="Name" SelectedItem="{Binding EmulatorProfile, Mode=TwoWay}"/>

                                                    <TextBlock Text="Platform" Grid.Row="2" Grid.Column="0" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <ComboBox Name="ComboPlatform" Grid.Row="2" Grid.Column="1" Margin="3,2,0,2" DockPanel.Dock="Left" IsTextSearchEnabled="True" ItemsSource="{Binding AvailablePlatforms}" DisplayMemberPath="Name" SelectedItem="{Binding RomMPlatform, Mode=TwoWay}"/>

                                                    <TextBlock Text="Path" Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <TextBox x:Name="Path" Grid.Row="3" Grid.Column="1" Text="{Binding Path=DestinationPath, Mode=TwoWay}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <Button Click="Click_BrowseDestination" Grid.Row="3" Grid.Column="2" Margin="0,0,10,0">
                                                        <TextBlock Text="Browse"/>
                                                    </Button>

                                                    <TextBlock Text="Enabled" Grid.Row="4" Grid.Column="0" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <CheckBox Grid.Row="4" Grid.Column="1" Width="30" HorizontalAlignment="Left" IsChecked="{Binding Enabled, Mode=TwoWay}"/>

                                                    <TextBlock Text="AutoExtract" Grid.Row="1" Grid.Column="3" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <CheckBox Grid.Row="1" Grid.Column="4" Width="30" HorizontalAlignment="Left" IsChecked="{Binding AutoExtract, Mode=TwoWay}" ToolTip="Will extract ROMs if they are stored on the server in Rar, Zip, Tar, 7z, GZip, or BZip2 format!" />

                                                    <TextBlock Text="Prefer .m3u" Grid.Row="2" Grid.Column="3" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <CheckBox Grid.Row="2" Grid.Column="4" Width="30" HorizontalAlignment="Left" IsChecked="{Binding UseM3U, Mode=TwoWay}" ToolTip="Will use .m3u file if multi-file ROM and emulator supports .m3u!"/>

                                                    <TextBlock Text="Mapping ID" Grid.Row="0" Grid.Column="3" Margin="0,0,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <TextBox Grid.Row="0" Grid.Column="4" Margin="0,0,10,0" IsReadOnly="True" HorizontalAlignment="Left" Text="{Binding MappingId, Mode=OneWay}"/>

                                                    <Button Grid.Row="0" Grid.Column="5" Content="❌" Tag="{Binding}" ToolTip="Delete mapping!" Click="Click_Delete" Foreground="Red" Width="26" Height="26" Margin="0" Padding="0" />
                                                </Grid>
                                            </Expander>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Button Name="AddMapping" Click="Click_AddMapping" Grid.Row="0" Grid.Column="0" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <TextBlock Text="Add Mapping"/>
                                </Button>
                                <Button Name="GetPlatforms" Click="Click_PullPlatforms" Grid.Row="0" Grid.Column="2" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Top">
                                    <TextBlock Text="Sync RomM Platforms"/>
                                </Button>
                            </Grid>

                            <!-- Library scanning -->
                            <TextBlock Text="Library scanning" FontWeight="Bold" Margin="0,10,0,0" />
                            <Separator Margin="0,5,0,10" />

                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Exclude Genres" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5"/>
                                <Grid>
                                    <TextBox  Width="250"  VerticalAlignment="Center" HorizontalAlignment="Left" x:Name="SearchTermTextBox" Margin="5" Text="{Binding ExcludeGenres, Mode=TwoWay}"/>
                                    <TextBlock IsHitTestVisible="False" Text="Adventure;Platform;RPG;" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="DarkGray">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SearchTermTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </StackPanel>
                            <CheckBox IsChecked="{Binding ScanGamesInFullScreen, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Enable game scanning in fullscreen mode" TextWrapping="Wrap"/>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding MergeRevisions, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Merge ROM revisions" TextWrapping="Wrap"/>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding KeepDeletedGames, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Keep games deleted from RomM server" TextWrapping="Wrap"/>
                            </CheckBox>
                            <CheckBox IsChecked="{Binding SkipMissingFiles, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Skip importing ROMs that are missing from RomM servers filesystem" TextWrapping="Wrap"/>
                            </CheckBox>

                            <!-- Installation -->
                            <TextBlock Text="Installation" FontWeight="Bold" Margin="0,10,0,0" />
                            <Separator Margin="0,5,0,10" />
                            <CheckBox IsChecked="{Binding NotifyOnInstallComplete, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Enable notification upon game install completion" TextWrapping="Wrap"/>
                            </CheckBox>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <CheckBox IsChecked="{Binding Use7z, Mode=TwoWay}" Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
                                    <TextBlock Text="Use 7z for archive extraction" TextWrapping="Wrap"/>
                                </CheckBox>
                                <TextBox x:Name="PathTo7zText" Grid.Row="0" Grid.Column="1" Margin="3,2,0,2" Text="{Binding PathTo7z, Mode=TwoWay}" />
                                <Button Name="Browse7z" Click="Click_Browse7zDestination" Grid.Row="0" Grid.Column="2">
                                    <TextBlock Text="Browse" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5"/>
                                </Button>
                            </Grid>

                            <!-- Status Syncing -->
                            <TextBlock Text="Status Sync" FontWeight="Bold" Margin="0,10,0,0" />
                            <Separator Margin="0,5,0,10" />
                            <CheckBox IsChecked="{Binding KeepRomMSynced, Mode=TwoWay}" Margin="0,0,0,10">
                                <TextBlock Text="Keep completion status and favorites in sync with RomM" TextWrapping="Wrap"/>
                            </CheckBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Save Panel -->
                <TabItem Name="Save"  Header="Manage Saves">
                    <ScrollViewer DockPanel.Dock="Top">
                        <StackPanel x:Name="SaveManagementPanel" DataContext="{Binding SaveController, Mode=OneWay}">
                            
                            <!-- Mapping Settings -->
                            <TextBlock Text="Saves" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                            <Separator Margin="0,5,0,10" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition Width="250"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="Select Mapping" Grid.Row="0" Grid.Column="0" Margin="10,2,0,2" FontSize="12" VerticalAlignment="Center"/>
                                <ComboBox Name="ComboMappings" Grid.Row="0" Grid.Column="1" Margin="3,2,0,2" DockPanel.Dock="Left" IsTextSearchEnabled="True" ItemsSource="{Binding Mappings}" DisplayMemberPath="MappingName" SelectedItem="{Binding CurrentMapping, Mode=TwoWay}"/>
                                <TextBlock Text="General Save Directory" Grid.Row="1" Grid.Column="0" Margin="10,2,0,2" FontSize="12" VerticalAlignment="Center" ToolTip="Select the top level save folder"/>
                                <TextBox x:Name="PathToSaves" Grid.Row="1" Grid.Column="1" Margin="3,2,0,2" Text="{Binding CurrentMapping.GeneralSavePath, Mode=TwoWay}" LostKeyboardFocus="LostKeyboard_GeneralSavePath"/>
                                <Button Grid.Row="1" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Click="Click_SaveDirectory" Margin="3,2,0,2" DataContext="{Binding CurrentMapping}" MinWidth="50" MaxWidth="100">
                                    <TextBlock Text="Browse" Margin="2"/>
                                </Button>

                                <CheckBox Grid.Row="0" Grid.Column="3" Margin="3,2,0,2" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding CurrentMapping.UploadSaveAfterGame, Mode=TwoWay}"/>
                                <TextBlock Text="Backup save on game exited" Grid.Row="0" Grid.Column="4" Margin="3,2,0,2" FontSize="12" VerticalAlignment="Center"/>

                                <CheckBox Grid.Row="1" Grid.Column="3" Margin="3,2,0,2" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="{Binding CurrentMapping.DownloadSaveBeforeGame, Mode=TwoWay}"/>
                                <TextBlock Text="Download newer save before game starts" Grid.Row="1" Grid.Column="4" Margin="3,2,0,2" FontSize="12" VerticalAlignment="Center"/>

                                <TextBlock Text="Savefile Extention(s)" Grid.Row="2" Grid.Column="0" Margin="10,2,0,2" FontSize="12" VerticalAlignment="Center"/>
                                <Grid Grid.Row="2" Grid.Column="1">
                                    <TextBox VerticalAlignment="Center" HorizontalAlignment="Stretch" x:Name="SaveTextBox" Margin="5" Text="{Binding CurrentMapping.SaveFileExtentions, Mode=TwoWay}" LostKeyboardFocus="FocusChanged_SaveExtentionsBox"/>
                                    <TextBlock IsHitTestVisible="False" Text="sav;srm; or leave empty for all files!" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Foreground="DarkGray">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, ElementName=SaveTextBox}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>

                            </Grid>
                            <Separator Margin="0,5,0,10" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="RomM Server Saves" FontWeight="Bold" Margin="0,5,0,0" Grid.Column="0"/>
                                <Button Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Click_SyncSaves">
                                    <TextBlock Text="Sync Remote Saves" Margin="2"/>
                                </Button>
                            </Grid>
                            
                           <!-- Remote Saves -->
                            <Separator Margin="0,5,0,10" />
                            <StackPanel x:Name="RemoteSaveFileStack">
                                <ItemsControl ItemsSource="{Binding RemoteSaves}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="3,2,0,2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="Gray" />
                                                        <Setter Property="BorderThickness" Value="1" />
                                                        <Setter Property="CornerRadius" Value="3" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotEnabled}">
                                                                <Setter Property="BorderBrush" Value="Gray"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.RemoteNewer}">
                                                                <Setter Property="BorderBrush" Value="Red"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.LocalNewer}">
                                                                <Setter Property="BorderBrush" Value="Yellow"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.InSync}">
                                                                <Setter Property="BorderBrush" Value="Green"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="110"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <CheckBox Grid.Row="0" Grid.Column="0" Margin="10,5,5,5" VerticalAlignment="Center" HorizontalAlignment="Left" IsChecked="{Binding SyncEnabled, Mode=TwoWay}" Checked="Checked_RemoteEnableSync" Unchecked="Checked_RemoteEnableSync"/>
                                                    <TextBlock Text="Enable" Grid.Row="0" Grid.Column="0" Margin="40,5,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                                                    <TextBlock Grid.Row="0" Grid.Column="3" Margin="5" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotEnabled}">
                                                                        <Setter Property="Foreground" Value="Gray"/>
                                                                        <Setter Property="Text" Value="Not Enabled"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.RemoteNewer}">
                                                                        <Setter Property="Foreground" Value="Red"/>
                                                                        <Setter Property="Text" Value="Remote Newer"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.LocalNewer}">
                                                                        <Setter Property="Foreground" Value="Yellow"/>
                                                                        <Setter Property="Text" Value="Local Newer"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.InSync}">
                                                                        <Setter Property="Foreground" Value="Green"/>
                                                                        <Setter Property="Text" Value="Synced"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <TextBlock Text="{Binding GameName, Mode=OneWay}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap"/>

                                                    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding FileName, Mode=OneWay}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap"/>
                                                        <TextBlock Text="{Binding LastUpdatedString, Mode=OneWay}" Margin="5,0,0,0" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="Gray"/>
                                                    </StackPanel>

                                                    <TextBlock Text="Save folder" Grid.Row="3" Grid.Column="0" Margin="10,5,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <TextBox Grid.Row="3" Grid.Column="1" Margin="5" FontSize="12" VerticalAlignment="Center" IsEnabled="{Binding SyncEnabled, Mode=OneWay}" Text="{Binding SaveFolder, Mode=TwoWay}"/>
                                                    <Button  Grid.Row="3" Grid.Column="2" Margin="5" VerticalAlignment="Center" Click="Click_BrowseSavefile" MaxWidth="100">
                                                        <TextBlock Text="Browse" Margin="2"/>
                                                    </Button>

                                                    <Button  Grid.Row="4" Grid.Column="3" Margin="5" VerticalAlignment="Center" Click="Click_SyncNow" MaxWidth="100">
                                                        <TextBlock Text="Sync now" Margin="2"/>
                                                    </Button>

                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="All saves synced with local!" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RemoteSaves.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                            <!-- Local Saves -->
                            <TextBlock Text="Local saves" FontWeight="Bold" Margin="0,5,0,0"/>
                            <Separator Margin="0,5,0,10" />
                            <StackPanel x:Name="LocalSaveFileStack">
                                <ItemsControl ItemsSource="{Binding LocalSaves}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="3,2,0,2">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="Gray" />
                                                        <Setter Property="BorderThickness" Value="1" />
                                                        <Setter Property="CornerRadius" Value="3" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotEnabled}">
                                                                <Setter Property="BorderBrush" Value="Gray"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.RemoteNewer}">
                                                                <Setter Property="BorderBrush" Value="Red"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.LocalNewer}">
                                                                <Setter Property="BorderBrush" Value="Yellow"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotUploaded}">
                                                                <Setter Property="BorderBrush" Value="Orange"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.InSync}">
                                                                <Setter Property="BorderBrush" Value="Green"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="3*"/>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="30"/>
                                                        <ColumnDefinition Width="30"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <CheckBox Grid.Row="0" Grid.Column="0" Margin="10,5,5,5" VerticalAlignment="Center" HorizontalAlignment="Left" IsChecked="{Binding SyncEnabled, Mode=TwoWay}" Checked="Checked_LocalEnableSync" Unchecked="Checked_LocalEnableSync"/>
                                                    <TextBlock Text="Enable" Grid.Row="0" Grid.Column="0" Margin="40,5,5,5" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <Button Grid.Row="0" Grid.Column="4" ToolTip="Remove save entry!" Click="Click_RemoveSave" Background="Transparent" Width="26" Height="26" Margin="0" Padding="0" HorizontalAlignment="Right" >
                                                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xecc9;" Foreground="Orange"/>
                                                    </Button>
                                                    <Button Grid.Row="0" Grid.Column="5" ToolTip="Delete save!" Click="Click_DeleteSave" Background="Transparent" Width="26" Height="26" Margin="0" Padding="0" HorizontalAlignment="Right" >
                                                        <TextBlock Text="❌" Foreground="Red"/>
                                                    </Button>

                                                    <Separator Grid.Row="1" Grid.ColumnSpan="10"/>

                                                    <TextBlock Grid.Row="2" Grid.Column="3" Grid.ColumnSpan="3" Margin="5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotEnabled}">
                                                                        <Setter Property="Foreground" Value="Gray"/>
                                                                        <Setter Property="Text" Value="Not Enabled"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.RemoteNewer}">
                                                                        <Setter Property="Foreground" Value="Red"/>
                                                                        <Setter Property="Text" Value="Remote Newer"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.LocalNewer}">
                                                                        <Setter Property="Foreground" Value="Yellow"/>
                                                                        <Setter Property="Text" Value="Local Newer"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.NotUploaded}">
                                                                        <Setter Property="Foreground" Value="Orange"/>
                                                                        <Setter Property="Text" Value="Not Found on Server"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsInSync, Mode=OneWay}" Value="{x:Static enum:SaveSyncStatus.InSync}">
                                                                        <Setter Property="Foreground" Value="Green"/>
                                                                        <Setter Property="Text" Value="Synced"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <TextBlock Text="{Binding GameName, Mode=OneWay}" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap"/>

                                                    <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding FileName, Mode=OneWay}" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap"/>
                                                        <TextBlock Text="{Binding LastUpdatedString, Mode=OneWay}" Margin="5,0,0,0" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="Gray"/>
                                                    </StackPanel>

                                                    <TextBlock Text="Save folder" Grid.Row="4" Grid.Column="0" Margin="10,5,5,5" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <TextBox Grid.Row="4" Grid.Column="1" Margin="5" FontSize="14" VerticalAlignment="Center" IsEnabled="{Binding SyncEnabled, Mode=OneWay}" Text="{Binding SaveFolder, Mode=TwoWay}"/>
                                                    <Button  Grid.Row="4" Grid.Column="2" Margin="5" VerticalAlignment="Center" Click="Click_BrowseSavefile" MaxWidth="100">
                                                        <TextBlock Text="Browse" Margin="2"/>
                                                    </Button>

                                                    <Button  Grid.Row="5" Grid.Column="3" Margin="4" Grid.ColumnSpan="3" VerticalAlignment="Center" HorizontalAlignment="Right" Click="Click_SyncNow" MinWidth="75" MaxWidth="100">
                                                        <TextBlock Text="Sync now" Margin="2"/>
                                                    </Button>

                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="No local saves synced!" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LocalSaves.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                            <!-- Potential Saves -->
                            <TextBlock Text="Possible saves" FontWeight="Bold" Margin="0,5,0,0"/>
                            <Separator Margin="0,5,0,10" />
                            <StackPanel x:Name="PotentialSaveFileStack">
                                <ItemsControl ItemsSource="{Binding PossibleSaves}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="3,2,0,2">
                                                <Border.Style>
                                                    <Style TargetType="Border" >
                                                        <Setter Property="BorderBrush" Value="Gray" />
                                                        <Setter Property="BorderThickness" Value="1" />
                                                        <Setter Property="CornerRadius" Value="3" />
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="150"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Text="{Binding File.DirectoryName}" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,5,5,5" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding File.Name, Mode=OneWay}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="Wrap"/>
                                                        <TextBlock Text="Last Modified:" Margin="5,0,0,0" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="Gray"/>
                                                        <TextBlock Text="{Binding File.LastWriteTime, Mode=OneWay}" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Left" Foreground="Gray"/>
                                                    </StackPanel>

                                                    <TextBlock Text="Matching Game:" Grid.Row="2" Grid.Column="0" Margin="10,5,5,5" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                    <ComboBox Name="ComboGame" Grid.Row="2" Grid.Column="1" Margin="3,2,0,2" MaxHeight="20" DockPanel.Dock="Left" ItemsSource="{Binding DataContext.FilteredROMs, ElementName=SaveManagementPanel}" DisplayMemberPath="FileName" SelectedItem="{Binding Game, Mode=TwoWay}"/>

                                                    <Button  Grid.Row="4" Grid.Column="2" Margin="15,5,5,5" VerticalAlignment="Center" Click="Click_UploadSave" MaxWidth="100">
                                                        <TextBlock Text="Upload Save" Margin="2"/>
                                                    </Button>

                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="All files accounted for!" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PossibleSaves.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
            
           
        </Grid>
    </DockPanel>
</UserControl>
